import React from 'react';

import RoutesConfig from './RoutesConfig/RoutesConfig';

const App = function () {
  return (
    <div className="App">
      <RoutesConfig />
    </div>
  );
};

export default App;
